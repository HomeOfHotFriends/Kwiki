# Atomic Systems

This directory will contain one JSON file per atomic system, each representing a minimal, self-contained logic or language unit. These are not compilers or interpreters, but simulated logic systems—fantasy languages for generative play.
